import 'package:flutter/material.dart';

class FlatHeelsContainer extends StatelessWidget {
  const FlatHeelsContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/flat_heals.png'),
    );
  }
}